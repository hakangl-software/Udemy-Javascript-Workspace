

//? STRING METOTLARI

/*
    charAt()        : Verdiğin index numarasına göre sana karşılığındaki karakteri verir.
    concat()        : İki string ifadeyi birleştirir. 
    indexof()       : Vermiş olduğun değerin index numarasını verir.
    lastindexof()   : Son 
    toUpperCase()   : Dizideki bütün harfleri büyük harfe çevirir.
    toLowerCase()   : Dizideki bütün harfleri küçük harfe çevirir.
    trim()          : Dizideki boşlukları kırpar.
    slice()         : Dilimleme işleminde kullanılır.
    sunstring()     : Slice metodu ile aynı şey sadece index farkı vardır.
    replace()       : Yer değiştirmek anlamı var. String değerlerini değiştirip return eder.
    split()         : Dizideki belirlemiş olduğunuz ayraça göre ayırıp dilim dilim dizi haline getirir.
    valueOf()       :
    startSwith()    : Dizinin verdiğiniz değerle başlayıp başlamadığını gösterir. Boolean döner.
    endSwith()      : Dizinin verdiğiniz değerle bitip bitediğini gösterir. Boolean döner.


*/

    let kurs = "Modern Web Geliştirme Kursu";
    let tarih = "2025";


    // CHARAT METOT KULLANIMI
    // let karakter = kurs.charAt(5);
    // console.log(karakter);

    // CONCAT METOT KULLANIMI
    //let sonuc = kurs.concat(tarih);
    //console.log(sonuc);
    //Burada stringleri birleştirir. Kopyasını return ederç tanımladığım sonuca aktarır.

    // INDEXOF METOT
    // let index = kurs.indexOf("o");
    // console.log(index);

    // LASTINDEXOF METOT
    // let sonIndex = kurs.lastIndexOf("s");
    // console.log(sonIndex);

    // TOUPPERCASE METOT
    // let buyukHarf = kurs.toUpperCase();
    // console.log(kurs);
    // console.log(buyukHarf);

    // TOLOWERCASE METOT
    // let kucukHarf = kurs.toLowerCase();
    // console.log(kurs);
    // console.log(kucukHarf);

    // TRIM METOT
    //console.log(kurs.trim());

    // SLICE METOT
    // console.log(kurs);
    // console.log(kurs.slice(7,10)); // Başlangıç ve bitiş index numarasını (dahil) yazılır.

    // SUBSTRING METOT
    // console.log(kurs);
    // console.log(kurs.substring(0,5)); // Başlangıç ve bitiş index numarasını (hariç) yazılır.

    // REPLACE METOT
    // console.log(kurs);
    // console.log(kurs.replace("Modern","Güncel"));

    // SPLIT METOT
    // console.log(kurs);
    // let dizi = console.log(kurs.split(" "));
    // console.log(dizi);

    // STARTSWITH METOT
    // console.log(kurs);
    // console.log(kurs.startsWith("M"));

    // ENDSWITH METOT
    // console.log(kurs);
    // console.log(kurs.endsWith("u"));




